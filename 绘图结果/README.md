# 绘图结果

这里是本地查看图件的统一入口，不是生产发布目录。

- `网站图件/`：链接到 `data/current/maps/`；保留原网页路径，不移动底层文件。
- `天河历史图件/`：链接到 `data/tianhe/current/maps/`；兼容历史网页，未重新下载。
- `参考图片/`：用户提供的 EC 风场、气温参考图。
- `.历史核验/`：早期图面核验、试验图、标志试验，隐藏且仅本地保留。

本地已有的历史产品不代表线上最新产品。正式新图仍由寰生成并上传 OSS，本次没有下载新的图片或 wrfout。
以上目录内容都由 `.gitignore` 排除（本说明除外）；网站 `data/` 的原追踪规则未改变。
今后的人工审阅图可放在本目录下，以服务或具体试验命名；不要把审阅图复制进网页公开目录。
