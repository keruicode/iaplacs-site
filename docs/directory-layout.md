# Website 目录说明

更新：2026-09-26。本地工作区和寰生产脚本已整理；寰上的所有图件目录保持原位。

## 日常使用

| 内容 | 位置 | 说明 |
| --- | --- | --- |
| 网页主体 | 根目录 HTML/JS/CSS、各服务目录、`assets/` | 公共路径保持原样 |
| 当前脚本 | `tools/` | 见其中的脚本索引；共享路径模块保持生产图件输出位置不变 |
| 历史脚本 | `tools/archive/` | 天河过渡和一次性任务；不直接执行 |
| 图件查看入口 | `绘图结果/` | 参考图实际集中存放；公开图件通过链接浏览 |
| 网页清单和公开图件 | `data/` | 不搬动，防止链接或 catalog 生成失效 |
| 评分服务 | `cloud/airport-ratings-fc/` | 独立部署包，保留原结构 |
| 运维说明 | `docs/` | 当前运维及历史专项说明 |
| 本地新手教程 | `小白教程/` | 继续忽略 Git；未移动教程和构建依赖 |

## 隐藏的维护文件

按本轮“只保留主体”的要求，续接记录、最终答复记录、搬移清单、备份和排障记录集中在 `.codex/`，不上传 GitHub。
续接文件为 `.codex/Codex_resume_iaplacs_website.md`，沿用原文件和线程，不另建第二份。
最终答复分组沿用原记录，位置改为 `.codex/tmp/`；新答复仍插在对应文件标题后，续接新记录仍追加在文末。
`.tmp/` 仅保留历史兼容链接。历史脚本快照和核验图分别藏在 `tools/archive/.runtime-snapshots/` 与 `绘图结果/.历史核验/`。

## 备份与回退

整理前的完整压缩包为 `.codex/备份/Website_整理前_20260926.tar.gz`，包含 `.git`、未提交修改、全部本地图件和教程。
对应 `.sha256`、逐文件清单与校验结果在同一目录。本次备份不包含新建的 `.codex/`，避免将备份装入自身。
回退时先停止本地编辑，在另一个空目录执行 `tar -xzpf <压缩包绝对路径> -C <空目录>`，检查后再替换需要恢复的文件；不要直接整包覆盖工作区或生产目录。
本地搬移映射保存在 `.codex/目录迁移计划.json`，可逐项找回旧位置。

## 寰生产脚本已分类，图件不迁移

生产目录仍为 `/data1/elpt_2022_00083/kerui/Website`：

- `scripts/`：42份当前脚本实体，含Python、Shell、NCL及两个共享模块；与本地/GitHub的`tools/`对应。
- `archive/scripts/`：12个退役或一次性脚本，与`tools/archive/iap-legacy/`对应。归档不代表可以直接重新执行。
- 根目录不再保留52个脚本和兼容链接；8条网站crontab的入口统一改到`scripts/`，时间表不变。
- `runtime_paths.sh`区分脚本路径和数据根目录，内部调用走`scripts/`，图件、日志、状态仍走原位置。
- `scripts/.runtime-root`指向生产根目录；字体查找兼容脚本目录及其父目录。不要删除这个隐藏标记。
- 绘图输出、历史测试图、SHP、fonts、锁、状态和日志原路径不变；本轮不迁移、不清理任何图件。

整理前核心备份位于生产目录外：
`/data1/elpt_2022_00083/kerui/.codex_records/Website目录整理_20260926/Website_核心整理前.tar.gz`。
约170MB，包含脚本、配置、Git元数据、SHP/fonts、状态及原备份目录；相邻保存五个登录/出口节点的crontab快照、SHA256和逐项迁移表。
这是用户确认的核心备份，不是144GB图件全量备份。已完成试解压及逐文件比对。

每日`backup_iap_runtime.sh`会解引用兼容链接，压缩包内存放实际脚本正文，避免恢复出指向不存在目录的链接。
回退时先在独立空目录解压核心包，按迁移表选择需要的文件；不要直接解压覆盖生产树、图件或正在运行的脚本。
清理顶层兼容链接后，login02的8条任务只更新入口路径，时间表、参数和日志路径不变；server02的4条模式任务不变。英文化和清理的追加备份在`.codex_records/website_ascii_paths_20260926/`。

今后新增脚本、归档和配置文件名使用英文ASCII；不为改名而移动图件，既有中文SHP等数据文件名保持原样。

## zhoubj下的临时手动副本

用户指定的 `/data1/elpt_2022_00083/zhoubj/webscripts` 仅保存2026-09-26降水绘图脚本、SHP和字体的临时副本；不会自动跟随生产更新。
按后续要求，可见层现仅保留六份可直接运行的 NCL、SHP 和 fonts；`.备份/` 仅保留压缩包及校验文件。
该目录不再放说明、参数参考、Shell或Python文件（旧版本只在压缩包内留底）。
NCL手动副本默认读取当前目录的wrfout及SHP，生成单张PNG，不自动拼图或发布。
新增独立入口为 `rain_worknx_xinjiang_hour_bjt.ncl`、`rain_worknx_national_accum_12h_bjt.ncl`、`rain_worknx_national_accum_24h_bjt.ncl`，无需另设产品选择参数；累计入口按当前d01范围画图，跳过前12小时，只保留BJT 08/20端点的完整时段。六份NCL的开头统一为一行英文输出目录注释。
这些默认路径和图题调整仅用于临时副本，不能覆盖网站生产NCL。不含完整WRF或账号凭据，不从这里运行自动发布。
除这个明确指定的备份目录外，没有修改 `zhoubj` 下的模型数据或脚本。
