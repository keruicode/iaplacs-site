# 中国中东部服务切换

原中国中部与中国东南部合并为中国中东部，沿用宁夏服务 ID、首页和历史宁夏产品。
上饶网址跳转首页；上饶历史从公开目录清单移除。云南和新疆服务保持原配置。

## 数据与首发

- 新源目录：`/data1/elpt_2022_00083/zhoubj/WORK`。
- 首个允许的新起报：`2026091218`，即北京时间 2026-09-13 02:00。
- 必须在 `rsl.error.0000` 最后 200 行检测到 `SUCCESS COMPLETE WRF` 才绘图。
- `2026091212` 以及更早 WORK 个例不参与新服务；历史宁夏结果保留，不用 WORK 重画。
- 宁夏早报半成品发布停用；新疆原有早报逻辑保留。

## 图件

三个子按钮为宁夏区域、中国中东部、冰雹预警。宁夏区域使用原宁夏市县框。
中国中东部及冰雹大区域由输入 `XLONG/XLAT` 极值向外取整决定边界，避免扩域后被旧全国图框裁切。
9月12日新网格参考范围为约 66.81--172.19E、5.62--64.93N。
为避免150E以东西太平洋的大块无模拟区域压缩中国主体显示，中国中东部及冰雹预警图统一截到150E；
宁夏区域图不变。
小时与累计降水继续使用既有计算和 T12 spin-up 规则。

## 自动运行

### 2026-09-13 定时上线

本节记录历史上线过程；一次性切换任务现已停用并归档，不再运行。

正式入口为 `https://iaplacs.xyz/`。临时预览曾使用
`preview-central-east.html` 和 `preview-central-east.js`；正式页面上线后已删除。

北京时间 2026-09-13 11:30 前保留正式页面原导航。寰 login02 当前用户 crontab
每5分钟调用 `activate_central_east_20260913.sh`，脚本内部禁止早于11:30执行。
11:30开始经 server02 推送正式导航与上饶清单停用；Pages 构建完成后实际生效。
失败将在当日后续5分钟节点重试，成功写入
`logs/central-east-20260913.released`，之后不重复提交。
日志为 `logs/central-east-release.log`。此任务无需 Mac 或本地 VPN 在线。

切换脚本锁定 GitHub 发布仓库，恢复已审阅提交 `4d222c21` 的6个前端文件，
仅清空实时清单的上饶服务，不回退宁夏、云南或新疆新到的数据。
恢复的文件是 `index.html`、`ningxia/index.html`、`shangrao/index.html`、
`airpots/index.html`、`xinjiang/index.html`、`app.js`；上线前若再次修改这些文件，
必须同步调整切换脚本引用的提交，避免定时操作覆盖新修改。

宁夏发布器与自检入口也设有11:30门槛；页面上线与模式完成是独立条件。
如果11:30模式尚未完成，先保留历史宁夏产品，待完整结果到达后自动发布，
不允许提前发布半成品。历史切换脚本现存于
`Website/archive/scripts/activate_central_east_20260913.sh`，仅用于留档，不再执行。

上线完成后，可从用户 crontab 移除带 `central-east-release-20260913` 注释的
一次性任务；保留常规自检与其它服务任务。

自2026-09-26起，`audit_iap_forecast_publication.sh` 每2分钟（`*/2 * * * *`）检查完成的 WORK 个例，通过
`publish_worknx_ningxia_to_github.sh` 绘图并经 server02 发布 OSS 与 GitHub 清单。
巡检和服务发布器保留非阻塞锁：同一任务仍在执行时跳过本轮，结束后由下一轮继续检查。
原上饶提交、绘图和发布入口已停用；模式计算任务和 server02 模式启动 crontab 未修改。

寰上实际绘图脚本均在 `/data1/elpt_2022_00083/kerui/Website/scripts/`：

- `render_worknx_ningxia_overview.sh`：WORK降水绘图总入口，负责调用NCL、时段处理和拼图。
- `rain_worknx_ningxia_hour_bjt.ncl`：宁夏区域降水。
- `rain_worknx_national_hour_bjt.ncl`：中国中东部小时降水、冰雹和12/24小时累计降水。
- `publish_worknx_ningxia_to_github.sh`：调用绘图入口并完成OSS/GitHub发布。

文件名沿用 `worknx`，实际默认数据源为 `/data1/elpt_2022_00083/zhoubj/WORK`；
本地源码对应项目 `tools/` 下的同名文件。

备份位于寰 `/data1/elpt_2022_00083/kerui/Website/script_archive/central_east_20260913/`。
只恢复网站入口不能恢复旧数据归属：如回退，必须同时核对 WORK 用途、网页清单及定时任务。
